to be cost effective 
1.we have to monitor everyone using the aws properly
as a devops engineer one of our primary responsibility is to make the cost effective

for example a company example.com will track the use of
ec2
s3
lambda
IAM

using scripts the devops engineers will report to the reporting dashboard

lets say we will writing a shell script we need to create a file in that we need to keep all the
all the information in that and file and we need to do that everyday this shell script is integrated 
with a cronjob 
what is a cronjob ?
if you create a cronjob it will automatically runs the shellscript at that paarticular time


#!/bin/bash

################
#Author:K.sumakar
#DATE:6/27/2026
#
#Version:V1
#
#This script will report the aws usage

# aws s3
# aws ec2
# aws lambda
# aws Iam users

# list s3 buckets
echo "print list of s3"
aws s3 ls

#List Ec2 Instances
echo "print list of ec2"

aws ec2 describe-instances

#List Lambda
echo "print list of lambda"

aws lambda list-functions

#List Iam users
echo "print list of IAM"

aws iam list-users


aws ec2 describe-instances | jq '.Reservations[].Instances[].InstanceId'